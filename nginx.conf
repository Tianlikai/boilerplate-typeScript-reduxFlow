server {
    listen: 8080;
    server_name localhost;
    #charset koi8-r;
    #access_log  logs/anyconsole.access.log  main;
    #error_log  logs/anyconsole.error.log warn;
    error_page  404              /404.html;
    location / { 
        index index.html;
        root  /home/lktian/TBoilerplate/dist;
    }

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /500.html;
    # location = /50x.html {
    #    root   html;
    #}
}